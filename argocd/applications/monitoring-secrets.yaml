apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: monitoring-secrets
  namespace: argocd
  labels:
    app.kubernetes.io/name: monitoring-secrets
spec:
  project: default
  source:
    repoURL: https://github.com/nullcap/infra-k3s.git
    path: argocd/manifests/monitoring-secrets
    targetRevision: main
  destination:
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
