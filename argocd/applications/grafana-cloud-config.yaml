apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: grafana-cloud-config
  namespace: argocd
  labels:
    app.kubernetes.io/name: grafana-cloud-config
spec:
  project: default
  source:
    repoURL: https://github.com/nullcap/infra-k3s.git
    targetRevision: main
    path: argocd/manifests/grafana-cloud-config
  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
