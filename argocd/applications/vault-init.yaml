apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vault-init
  namespace: argocd
  labels:
    app.kubernetes.io/name: vault-init
spec:
  project: default
  source:
    repoURL: https://github.com/nullcap/infra-k3s.git
    path: argocd/manifests/vault-init
    targetRevision: main
  destination:
    namespace: vault
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
